# @flat-earth/core-sim

웹/모바일 공용 시뮬레이션 코어입니다.

## Exports

- `createSimulationState(config)`
- `advanceSimulation(state, dtSeconds, context)`
- `computeCelestialPositions(time, params)`
- `getHudState(state)`
- `applyCameraGesture(state, input)`

## Test

```bash
npm run test
```
